---
dir:
  - catalog
dynamic:
  enable: true
  data: product
  name: name
  slug: path
---

<%= product[locale].name %><br>
<%= number_to_currency(product.price.eur, :unit => "€") %><br>
<%= product[locale].description %><br>
<%= image_tag(product.image) %><br>
<%= snipcart_button product, t("snipcart.add_to_cart") %>
