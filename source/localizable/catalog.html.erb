---
slug: catalog
---

<% data.products.each_with_index do |b, index| %>
  <% link_to b[locale].path do %>
    <%= b[locale].name %>
    <%= number_to_currency(b.price.eur, :unit => "€") %>
  <% end %>
  <%= link_to 'Voir plus', b[locale].path, class: 'btn btn-main' %>
  <br><br>
<% end %>
